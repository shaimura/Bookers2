            <section>
				<h2>New book</h2>
				<%= form_for(Book.new) do |f| %>

				<h5>Title</h5>
				<%= f.text_field :title %>
				<h5>Opinion</h5>
				<%= f.text_area :body %>
				<%= f.submit "Create Book", class: "btn btn-primary" %>
				<% end %>

			</section>